# @lightui/group-taxonomy

Semantic grouping and card diet taxonomy: cards vs whitespace sections, form sections, activity feed, color bands, and price comparison.

## Development

```bash
make dev-study STUDY=group-taxonomy
# Or directly:
npm run dev --workspace=@lightui/group-taxonomy
```

Standalone playground runs at `http://127.0.0.1:5215/`.

# References

Local, generated, not committed.

Stills come from this study's stage (menu island only):

```
http://127.0.0.1:5173/s/intent-cascade/stage?kind=<id>&state=open
```

Kinds: `status` `diagonal` `project` `third`.
Files: `<kind>.png`.

- `status` — 两列，蓝走廊钉在「状态」
- `diagonal` — 途经「负责人」，走廊仍钉在「状态」
- `project` — 纵向切到「项目属性」后的蓝走廊
- `third` — 三列，已进入「紧急」（绿走廊）

```
make stills PROJECT=intent-cascade
```

Standalone: `http://127.0.0.1:5174/?stage=1&kind=diagonal&state=open`

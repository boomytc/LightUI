# @lightui/sheet-snap

Multi-snap bottom sheet with velocity projection and overdrag damping.

## Development

```bash
npm run dev --workspace=@lightui/sheet-snap
# or
make dev-study STUDY=sheet-snap
```

Runs the standalone playground at `http://127.0.0.1:5216/`.
Add `?stage=1` to view the stage fixture.

# inverted-notch

内凹角在父级挖孔。同色补丁缝回去，背景一变缝就露馅。

理念说明见 [idea.md](idea.md)。

## 运行

```bash
npm install
npm run dev
```

打开 `http://127.0.0.1:5178/`。

在 Lab 里打开：`make dev` → `http://127.0.0.1:5173/s/inverted-notch`。

悬停锁标看孔跟着变宽。打开分解视图，斜纹是被裁掉的角。切到 scoop 对照：只能凹一角，挖不出锁标孔。

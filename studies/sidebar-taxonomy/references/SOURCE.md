# References

Local, generated, not committed.

Stills come from this study's stage (no lab chrome):

```
http://127.0.0.1:5173/s/sidebar-taxonomy/stage?kind=<id>&state=closed|open
```

Kinds: `floating` `wheel` `multilevel` `collapsible` `offcanvas`.
Files: `<kind>.png` · `offcanvas-open.png`.

```
make stills PROJECT=sidebar-taxonomy
```

Standalone: `http://127.0.0.1:5176/?stage=1&kind=offcanvas&state=open`

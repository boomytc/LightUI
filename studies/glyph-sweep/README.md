# glyph-sweep

扫光跟字形走。光带宽度用 ch，时长等于字数乘速度。

理念说明见 [idea.md](idea.md)。

## 运行

```bash
npm install
npm run dev
```

打开 `http://127.0.0.1:5179/`。

在 Lab 里打开：`make dev` → `http://127.0.0.1:5173/s/glyph-sweep`。

点一行改字，时长应跟着字数变。打开「停住光带」把高光停在字形上。

# look-quantize

指针偏了，视线落到图集格子。半径里平滑，格子外夹紧，眨眼走另一行。

理念说明见 [idea.md](idea.md)。

## 运行

```bash
npm install
npm run dev
```

打开 `http://127.0.0.1:5180/`。

在 Lab 里打开：`make dev` → `http://127.0.0.1:5173/s/look-quantize`。

移动指针，脸应落到离散格子，而不是连续转头。打开「显示格子」能看见 13×3 的颗粒。换格硬切，不叠化。
